import 'package:flutter/material.dart';

class CartPageTablet extends StatelessWidget {
  const CartPageTablet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
