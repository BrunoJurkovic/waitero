import 'package:flutter/material.dart';

class CartPageMobile extends StatelessWidget {
  const CartPageMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}